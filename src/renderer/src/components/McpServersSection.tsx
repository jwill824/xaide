import type { FC } from 'react'
export const McpServersSection: FC<{ workspaceId: string | null }> = () => (
  <div><h1 className="text-lg font-semibold text-neutral-100">MCP Servers Configuration</h1></div>
)
